baseURL = "https://main.d6cqd73wjarhj.amplifyapp.com/"
DefaultContentLanguage = "ja"
languageCode = "ja-JP"
hasCJKLanguage = true
paginate = "10" # Number of posts per page
title = "Muichimotsu"
theme = "mainroad"
disqusShortname = "muichimostu-com" # Enable Disqus comments by entering your Disqus shortname
googleAnalytics = "" # Enable Google Analytics by entering your tracking id

[Author]
name = "Hiropon"
bio = "ゲーム全般が好き"
avatar = "" # avatar image

[Params]
  description = "ゲームとか趣味のものいろいろ" # Site description. Used in meta description
  copyright = "Hiropon" # Footer copyright holder, otherwise will use site title
  opengraph = false # Enable OpenGraph if true
  schema = false # Enable Schema
  twitter_cards = true # Enable Twitter Cards if true
  readmore = false # Show "Read more" button in list if true
  authorbox = false # Show authorbox at bottom of pages if true
  toc = true # Enable Table of Contents
  pager = true # Show pager navigation (prev/next links) at the bottom of pages if true
  post_meta = ["author", "date", "categories", "translations"] # Order of post meta information
  mainSections = ["post", "blog", "news"] # Specify section pages to show on home page and the "Recent articles" widget
  dateformat = "2006-01-02" # Change the format of dates
  mathjax = false # Enable MathJax
  mathjaxPath = "https://cdn.jsdelivr.net/npm/mathjax@3/es5/mml-svg.js" # Specify MathJax path
  mathjaxConfig = "" # Specify MathJax config
  customCSS = ["css/custom.css?4"] # Include custom CSS files
  # customJS = ["js/custom.js"] # Include custom JS files

[Params.style.vars]
  highlightColor = "#224466" # Override highlight color

[Params.logo]
  image = "" # Logo image. Path relative to "static"
  title = "無一物" # Logo title, otherwise will use site title
  subtitle = "趣味の記録をまとめるサイト" # Logo subtitle

[Params.thumbnail]
  visibility = ["list"] # Control thumbnail visibility

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist", "social", "languages"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  categories_counter = true # Enable counter for each category in "Categories" widget
  tags_counter = true # Enable counter for each tag in "Tags" widget (disabled by default)

[Params.widgets.social]
  # Enable parts of social widget
#   facebook = "username"
  twitter = "teetee3319"
#   instagram = "username"
#   linkedin = "username"
#   telegram = "username"
#   github = "username"
#   gitlab = "username"
#   bitbucket = "username"
  email = "skima.fork@gmail.com"

[menu]
[[menu.main]]
  identifier = "Disco Elysium"
  name = "Disco Elysium"
  url = "/categories/disco-elysium"
  weight = 10

[[menu.main]]
  identifier = "about"
  name = "About"
  url = "/post/about"
  weight = 1000

[markup.goldmark.renderer]
  unsafe = true